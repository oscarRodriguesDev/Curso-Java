package br.com.aed.Logica;

/*declaramos a clsse usando a palavra reservada public, seguido da palvra reservada class,
 seguido
 * do nome da classe de nossa escolha*/
public class OlaMundo {
	/**
	 * Esta classe destina-se a demonstrar a estrutura basica de um programa java
	 */
	/*
	 * abaixo o metodo main(principal) este metodo � o responsavel por rodar
	 * qualquer programa java e todo programa java se inicia pelo metodo main, que
	 * recebe como argumento um array de Strings
	 */
	public static void main(String[] argumento) {
		/*
		 * abaixo temos um metodo muito usado quando se estuda a linguagem java o
		 * System.out.print()que imprime no prompt de comando ou no console do IDE i
		 * informa��o passada como parametro
		 */
		System.out.print("ola mundo java!!!");
		/*
		 * um detalhe importante, todo o comando em java deve ser finalizado com o sinal
		 * ';'
		 */

	}
	/*
	 * note que o program se inicia com uma chave, depois no metodo main abre outra
	 * chave, e aqui no final da classe fechamos as duas chaves
	 */
}
